import React from 'react';

// Empty component that doesn't render anything
// The registration button should ONLY exist in the Hero section at the top of the page
const Registration: React.FC = () => {
  // Return null so this component doesn't render anything
  return null;
};

export default Registration;